# ── Frontend / Static Server ──────────────────────────────────
# Optional. Port for the production static file server.
PORT=8787

# ── Backend API ───────────────────────────────────────────────
# Port the Express REST API listens on.
API_PORT=4000

# Secret key used to sign JWT tokens. Change this in production!
JWT_SECRET=dhanvika_boutique_secret_change_me

# SQLite database file path (relative to project root).
DB_PATH=./backend/db/boutique.db

# Optional: allowed CORS origin for production deployments.
# CORS_ORIGIN=https://yourdomain.com
